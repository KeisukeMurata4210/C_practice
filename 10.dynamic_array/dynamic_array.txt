・配列は要素を変更できない

「malloc(バイトサイズ)」を実行すると、
必要なメモリを確保した上で先頭のアドレスを戻り値として返す。
メモリを確保できなければNULLを返す
そのアドレスをポインタ変数として受け取る。
そのポインタ変数を、[]を使って配列として扱う

ヒープ：　mallocで確保されたメモリ
動的配列：ヒープに確保された配列

free(ポインタ変数)でメモリを解放する
解放しなければプログラム終了まで残る